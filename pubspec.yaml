name: qr_code_generator
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 0.1.0

environment:
  sdk: '>=2.18.2 <3.0.0'

dependencies:
  auto_route: ">=5.0.0 <6.0.0"
  file_picker: ">=5.0.0 <6.0.0"
  flutter:
    sdk: flutter
  flutter_colorpicker: ">=1.0.0 <2.0.0"
  flutter_localizations:
    sdk: flutter
  get_it: ">=7.0.0 <8.0.0"
  intl: ">=0.0.0 <1.0.0"
  path: ">=1.0.0 <2.0.0"
  path_provider: ">=2.0.0 <3.0.0"
  qr: ">=2.0.0 <3.0.0"
  qr_flutter: ">=4.0.0 <5.0.0"
  share_plus: ">=6.0.0 <7.0.0"
  shared_preferences: ">=2.0.0 <3.0.0"

dev_dependencies:
  auto_route_generator: ">=5.0.0 <6.0.0"
  build_runner:
  flutter_test:
    sdk: flutter
  lint:

flutter:
  uses-material-design: true
  generate: true
